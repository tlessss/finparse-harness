# FinParseAI — 开源默认忽略「本地私密 + 运行产物」
# 说明：被 ignore 的文件仍保留在你磁盘上，本地开发不受影响；只是不进入 git。

# ── Python ──────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.tox/
.nox/
.hypothesis/

# 虚拟环境（根目录 + tests 下）
.venv/
venv/
env/
tests/venv/

# ── 环境 / 密钥（.env.example 可提交）──────────────
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
secrets.json

# ── 前端（Next.js）──────────────────────────────
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/.turbo/
frontend/*.tsbuildinfo

# ── 运行时数据（本地 goldset，不进公开仓）──────────
goldset/
run_state/
test_results/
/tmp/
experience_db.json

# 批量测试 / 校验报告
batch_test_results_*.json
*_校验报告.md
批量测试报告_*.md

# ── PDF 与原始财报（用 book-agent 缓存或自备，不公开）──
*.pdf
pdfs/
# llm-docs/ 下多为准则原文 PDF，默认不公开
llm-docs/

# ── 求职 / 个人（仅本地）──────────────────────────
docs/career/
简历/
MORNING_BRIEF.md

# ── IDE / 本地 Agent 配置（skills 目录请保留提交）──
.claude/
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/
!.vscode/extensions.json
!.vscode/settings.json.example

# ── 日志与临时文件 ────────────────────────────────
*.log
logs/
.cache/
camelot.log

# ── 可选：LLM 生成的认证解析器（含版式专用逻辑，默认不公开）
# 若开源需要 demo，可只提交 src/parsers/versions/rev_demo_*.py 并删掉下行
# src/parsers/versions/
