"""抽表器(table finder)层 —— 和解析器同构的独立层。

职责：给定全部表 + 字段，返回"该字段的目标表候选(按可信度降序)"。
  - 默认抽表器(default)：启发式(filter_by_signature)。
  - 专用抽表器(versions/)：默认漏表/选错表时，LLM 针对某版式写的选表器；认证 + 按
    fingerprint 路由，只对命中的报告覆盖默认 —— 绝不动默认、绝不误伤别的报告。

契约：一个抽表器模块暴露
    def find(tables, field, context=None) -> list[dict]
返回候选表列表(每个含 table/page/...，结构同 scan_pdf)，最可信的在前。下游解析器按序试，
锚验证自动选中对的那张。

选择即验证：抽表器选的表 → 解析器解析 → 过 DB 锚 ⇒ 该抽表器对。
运行期零 LLM：抽表器是确定性代码；LLM 只在构建期写它(失败诊断 → 写专用抽表器 → 回归认证)。
"""
